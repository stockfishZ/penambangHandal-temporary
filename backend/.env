# PostgreSQL / PostGIS Database Configuration
# Database is optional - backend runs in API-only mode without it
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=PenambangHandal
DB_NAME=geonirisk

# CORS Configuration
ALLOWED_ORIGINS=*
