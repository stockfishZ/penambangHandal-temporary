# PostgreSQL / PostGIS Database Configuration
# Optional: use DATABASE_URL instead of the individual DB_* fields.
# DATABASE_URL=postgresql://postgres:your_password@localhost:5432/geonirisk
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_NAME=geonirisk

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:5500,http://127.0.0.1:5500,http://localhost:3000
